name: Build Kernel`non: [push, workflow_dispatch]`njobs:`n  build:`n    runs-on: ubuntu-latest`n    steps:`n      - name: Checkout`n        uses: actions/checkout@v4`n      - name: Pull Source`n        run: |`n          git clone https://github.com/MotorolaMobilityLLC/kernel-msm --depth 1 source`n      - name: List Configs`n        run: |`n          ls source/arch/arm64/configs/`n      - name: Toolchains`n        run: |`n          echo "If we see the list above, we can pick the right defconfig next."
