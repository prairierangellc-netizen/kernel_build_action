name: Build Kernel`non:`n  workflow_dispatch:`n  push:`n    branches: [ main ]`njobs:`n  build:`n    runs-on: ubuntu-latest`n    steps:`n      - name: Checkout`n        uses: actions/checkout@v4`n      - name: Install Dependencies`n        run: |`n          sudo apt-get update`n          sudo apt-get install -y build-essential bc bison flex libssl-dev libelf-dev clang lld llvm`n      - name: Setup AOSP GCC Toolchains`n        run: |`n          mkdir -p prebuilts/gcc/linux-x86/aarch64`n          mkdir -p prebuilts/gcc/linux-x86/arm`n          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 -b master --depth 1 prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9`n          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 -b master --depth 1 prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9`n      - name: Build Kernel`n        run: |`n          make O=out ARCH=arm64 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=$(pwd)/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9/bin/aarch64-linux-android- CROSS_COMPILE_ARM32=$(pwd)/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9/bin/arm-linux-androideabi- -j$(nproc)
